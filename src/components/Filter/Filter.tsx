import React from "react";

function Filter(props: any) {
  // put filter state here as well. use passback to get data
  return <div>{props.children}</div>;

  // put submit button here
}

export default Filter;
